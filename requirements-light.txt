# Lightweight deps for the pieces that run on the Windows host / CPU / small GPU:
# data prep, DINOv2 embeddings, cross-encoder MTA, and the full calibration pipeline.
# (All already present in the dev environment; pinned loosely for reproducibility.)
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.2            # CUDA build recommended (e.g. +cu124) for DINOv2/cross-encoder
torchvision>=0.17     # CIFAR-10 download
transformers>=4.44    # DINOv2
sentence-transformers>=2.7   # ms-marco cross-encoder (MTA)
pillow>=10.0
pyyaml>=6.0
datasets>=2.18        # ChaosNLI (Phase 1.2)
pytest>=8.0
